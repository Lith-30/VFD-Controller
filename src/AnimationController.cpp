/*
    This animation controller will be specific to the simple VFD, not compatable with any other type. (Will add the model number later)
*/

#include "ByteSequence.h"

class AnimationController
{
private:
    /* data */
public:
    AnimationController(/* args */);
    ~AnimationController();
};

AnimationController::AnimationController(/* args */) {
    // init all the number maps
}

AnimationController::~AnimationController() {

}